extends layout

block content

  // Once you've added a content type and posts into Contentful
  // go update your app.coffee config and then test out iterating
  // over your content type here!
  .header
    .header-content
      p READYTO
  .cover
    .cover-content
      p We exist to create a new banking experience — one that is more diverse, open and fair.
      img(src= 'img/us.png', alt="test", class="cover__image")
      
  ul.blog-post__container
    - for post in contentful.posts
      li.blog-post
        img(src= asset(post.coverImage), alt="test", class="index__post-image")
        h4: a(href=post._url)=post.title
        p= post.content
        p= post.date
